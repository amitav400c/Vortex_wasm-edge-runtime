[server]
port = 8080
workers = 12
# cert_path = "cert.pem"
# key_path = "key.pem"

[rate_limit]
max_requests = 99999999999999
window_seconds = 60  # Future: implement sliding window

[modules]
# Pipeline modules to load on startup (optional)
# pipeline = ["auth.wat", "logic.wat"]
pipeline = []
